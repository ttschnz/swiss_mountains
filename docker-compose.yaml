services:
  swiss_mountains:
    build: .
    devices:
      - /dev/dri:/dev/dri
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./cache:/app/cache
      - ./gifs:/app/gifs